# Выборки для прогнозирования

CSV-файлы с временными рядами и тестовыми наборами для ` main.R ` и методов из ` AllMethods `.

` 1.csv ` … ` 6.csv `, ` Book2.csv `, ` periodtest.csv ` — разные наборы данных для сравнения методов прогноза;
