# Выборки данных

CSV-файлы с числовыми рядами и наблюдениями для скриптов из корня и подпапок предмета.

` data.csv `, ` data2.csv `, ` data3.csv ` и др. — разные наборы для тестов и лабораторных;

` 12.csv ` — дополнительная выборка;
